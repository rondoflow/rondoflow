{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["DOCS_ORIGIN", "NEXT_PUBLIC_API_URL", "NEXT_PUBLIC_SOCKET_URL"]
    },
    "start": {
      "cache": false,
      "persistent": true,
      "env": ["DOCS_ORIGIN", "NEXT_PUBLIC_API_URL", "NEXT_PUBLIC_SOCKET_URL", "NODE_ENV"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**", "out/**", "public/_pagefind/**"]
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "test": {
      "outputs": [],
      "inputs": ["src/**/*.ts", "src/**/*.tsx", "vitest.config.ts", "tsconfig.json"],
      "env": ["CI", "NODE_ENV"]
    },
    "test:coverage": {
      "outputs": ["coverage/**"],
      "inputs": ["src/**/*.ts", "src/**/*.tsx", "vitest.config.ts", "tsconfig.json"],
      "env": ["CI", "NODE_ENV"]
    }
  }
}
